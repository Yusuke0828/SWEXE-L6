<%= Product.find(@product_id).name %>
<%= Product.find(@product_id).price %>
<%= form_for @cart_item, action: :create do |f| %>
    <%= f.label :qty, '数量' %>
    <%= f.text_field :qty %>
    <%= f.hidden_field :product_id %>
    <%= f.submit '登録' %>
<% end %>